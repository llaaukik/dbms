SQL - 3
Design at least 10 SQL queries for  selected real time application database using SQL DML statements for all types of Join, Sub-Query and View.
1️⃣ Create and Use Database
CREATE DATABASE IF NOT EXISTS demo_db;
USE demo_db;

​
2️⃣ Create Tables
DROP TABLE IF EXISTS customers;
CREATE TABLE customers (
    customer_id INT AUTO_INCREMENT PRIMARY KEY,
    customer_name VARCHAR(50),
    city VARCHAR(50)
);

DROP TABLE IF EXISTS orders;
CREATE TABLE orders (
    order_id INT AUTO_INCREMENT PRIMARY KEY,
    customer_id INT,
    order_date DATE,
    amount DECIMAL(10,2),
    FOREIGN KEY (customer_id) REFERENCES customers(customer_id)
);

DROP TABLE IF EXISTS products;
CREATE TABLE products (
    product_id INT AUTO_INCREMENT PRIMARY KEY,
    product_name VARCHAR(50),
    price DECIMAL(10,2)
);

DROP TABLE IF EXISTS order_details;
CREATE TABLE order_details (
    order_detail_id INT AUTO_INCREMENT PRIMARY KEY,
    order_id INT,
    product_id INT,
    quantity INT,
    FOREIGN KEY (order_id) REFERENCES orders(order_id),
    FOREIGN KEY (product_id) REFERENCES products(product_id)
);

​
3️⃣ Insert Sample Data
-- Customers
INSERT INTO customers (customer_name, city) VALUES
('pratik', 'kopergoan'),
('rhutik', 'shirdi'),
('omm', 'rahata');

-- Products
INSERT INTO products (product_name, price) VALUES
('Laptop', 60000),
('Phone', 30000),
('Headphones', 2000);

-- Orders
INSERT INTO orders (customer_id, order_date, amount) VALUES
(1, '2025-09-20', 62000),
(2, '2025-09-21', 30000),
(3, '2025-09-22', 2200);

-- Order Details
INSERT INTO order_details (order_id, product_id, quantity) VALUES
(1, 1, 1),
(1, 3, 1),
(2, 2, 1),
(3, 3, 1);

​
4️⃣ Simple SELECT – all customers
SELECT * FROM customers;

​
5️⃣ INNER JOIN – show orders with customer names
SELECT o.order_id, c.customer_name, o.amount
FROM orders o
INNER JOIN customers c ON o.customer_id = c.customer_id;

​
6️⃣ LEFT JOIN – all customers and their orders
SELECT c.customer_name, o.order_id, o.amount
FROM customers c
LEFT JOIN orders o ON c.customer_id = o.customer_id;

​
7️⃣ RIGHT JOIN – all orders with customer names
SELECT c.customer_name, o.order_id, o.amount
FROM customers c
RIGHT JOIN orders o ON c.customer_id = o.customer_id;

​
8️⃣ FULL OUTER JOIN (simulate with UNION in MySQL)
SELECT c.customer_name, o.order_id, o.amount
FROM customers c
LEFT JOIN orders o ON c.customer_id = o.customer_id
UNION
SELECT c.customer_name, o.order_id, o.amount
FROM customers c
RIGHT JOIN orders o ON c.customer_id = o.customer_id;

​
9️⃣ CROSS JOIN – all combinations of customers and products
SELECT c.customer_name, p.product_name
FROM customers c
CROSS JOIN products p;

​
🔟 Subquery – customers who spent more than average order amount
SELECT customer_name
FROM customers
WHERE customer_id IN (
    SELECT customer_id
    FROM orders
    WHERE amount > (SELECT AVG(amount) FROM orders)
);

​
1️⃣1️⃣ View – high-value orders (>50,000)
CREATE VIEW high_value_orders AS
SELECT o.order_id, c.customer_name, o.amount
FROM orders o
JOIN customers c ON o.customer_id = c.customer_id
WHERE o.amount > 50000;

​
Query the view:
SELECT * FROM high_value_orders;

​
1️⃣2️⃣ Subquery with JOIN – show customer name and product names
SELECT c.customer_name, p.product_name, od.quantity
FROM order_details od
JOIN orders o ON od.order_id = o.order_id
JOIN customers c ON o.customer_id = c.customer_id
JOIN products p ON od.product_id = p.product_id
WHERE od.order_id IN (
    SELECT order_id FROM orders WHERE amount > 5000
);
